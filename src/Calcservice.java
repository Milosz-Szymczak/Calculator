class Calcservice {

    public void logicCalc() {
        String option = Sout.SCANNER.nextLine().toUpperCase();
        switch (option) {
            case "1", "DODAWANIE" ->{
            }
        }
    }

}
